"""BERTScore utility functions — tokenisation, embedding, greedy matching.

Ported from FER/utils.py (only the code paths actually used by the score() function).
"""

from __future__ import annotations
